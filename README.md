# Java-Caculator-with-Spring-Boot
This project creates two endpoints /home and /caculate that can handle home pages and 4 arithmetic caculations, running them on the Apache Tomcat server, initialized with SpringBoot
